<%= provide(:title, 'All users') %>
<h1>All Users</h1>

<%= will_paginate %>

<ul class = "users">
	<%= render @users %>	
</ul>

<%= will_paginate %>
<%= link_to "Log Out", session_path("current"), method: "delete" %> 

    


